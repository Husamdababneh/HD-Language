//#import "test.hd"

Entity :: struct {
	a : int = 5;
	b : int ;
	c : int ;	
};

funcSig :: proc (a : u16  = 2, a : u8) -> void
{

	{
		
		a : Entity;
		a := "Hi";
		a = 4;
		a.a = 4;
		a.b = 3;
		b :: (5 * 2 + (8 / (1 + 3) ));
	}
}

		