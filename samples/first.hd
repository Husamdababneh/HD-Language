#import "Basic"

#load "file.hd"


internal
add :: proc (a : u64 = b, b : u64 = 55, hh : int) -> (u64) {
	return a + b;
}



#scope internal  // this tells the compiler that everything under it is only accessed by the file

main :: proc () {

	a :: b;
	return add();	
}

